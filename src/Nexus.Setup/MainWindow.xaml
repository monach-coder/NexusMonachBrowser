<Window x:Class="Nexus.Setup.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Установка Nexus Monach" Width="560" Height="420"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource Bg}" Foreground="{StaticResource Fg}"
        WindowStyle="SingleBorderWindow">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,18">
            <TextBlock Text="NEXUS MONACH" FontSize="26" FontWeight="SemiBold" />
            <TextBlock Text="Приватный браузер с локальным AI · установка для текущего пользователя, без прав администратора"
                       FontSize="12" Foreground="{StaticResource Muted}" TextWrapping="Wrap" Margin="0,4,0,0" />
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock x:Name="StatusText" Text="Готов к установке" FontSize="14" Margin="0,0,0,8" />
            <ProgressBar x:Name="Progress" Height="10" Minimum="0" Maximum="100" />
            <TextBlock x:Name="DetailText" FontSize="11" Foreground="{StaticResource Muted}"
                       Margin="0,6,0,0" TextTrimming="CharacterEllipsis" />
        </StackPanel>

        <TextBox x:Name="LogBox" Grid.Row="2" IsReadOnly="True" TextWrapping="NoWrap"
                 FontFamily="Consolas" FontSize="11" BorderThickness="1"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="InstallButton" Content="Установить" Width="130" Padding="0,9"
                    Background="{StaticResource Accent}" Foreground="White" BorderThickness="0"
                    Cursor="Hand" Click="Install_Click" />
            <Button x:Name="LaunchButton" Content="Запустить браузер" Width="150" Padding="0,9" Margin="10,0,0,0"
                    Visibility="Collapsed" Cursor="Hand" Click="Launch_Click" />
            <Button x:Name="AiButton" Content="Загрузить AI-модели" Width="160" Padding="0,9" Margin="10,0,0,0"
                    Visibility="Collapsed" Cursor="Hand" Click="Ai_Click" />
        </StackPanel>
    </Grid>
</Window>
