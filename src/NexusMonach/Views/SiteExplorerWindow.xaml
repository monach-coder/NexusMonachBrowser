<Window x:Class="NexusMonach.Views.SiteExplorerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:models="clr-namespace:NexusMonach.Models"
        Title="Структура сайта — Nexus Monach" Width="1040" Height="700"
        MinWidth="760" MinHeight="480"
        Background="{StaticResource BackgroundBrush}" Foreground="{StaticResource TextBrush}"
        Icon="/Assets/nexus-monach.ico" WindowStartupLocation="CenterOwner"
        Style="{StaticResource GlassPopupWindow}">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Image Width="44" Height="44" Source="/Assets/nexus-monach-512.png" />
            <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="ОБОЗРЕВАТЕЛЬ САЙТА" FontSize="20" FontWeight="SemiBold" />
                <TextBlock x:Name="OriginText" Margin="0,3,0,0" FontSize="11"
                           Foreground="{StaticResource MutedTextBrush}" Text="Подготовка локального снимка…" />
            </StackPanel>
        </StackPanel>

        <Border Grid.Row="1" Margin="0,16,0,12" Padding="12,9" Background="{StaticResource SelectedBrush}"
                BorderBrush="{StaticResource AccentBrush}" BorderThickness="1" CornerRadius="8">
            <TextBlock x:Name="SummaryText" TextWrapping="Wrap" Foreground="{StaticResource AccentBrush}"
                       Text="Значения форм, cookies, storage, script/style и секретные параметры ссылок не читаются." />
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.46*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="0.54*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="6">
                <TreeView x:Name="SiteTree" Background="Transparent" BorderThickness="0"
                          SelectedItemChanged="SiteTree_SelectedItemChanged"
                          VirtualizingPanel.IsVirtualizing="True"
                          VirtualizingPanel.VirtualizationMode="Recycling">
                    <TreeView.Resources>
                        <HierarchicalDataTemplate DataType="{x:Type models:SiteTreeItem}"
                                                  ItemsSource="{Binding Children}">
                            <TextBlock Text="{Binding Title}" TextTrimming="CharacterEllipsis"
                                       ToolTip="{Binding Title}" />
                        </HierarchicalDataTemplate>
                    </TreeView.Resources>
                </TreeView>
            </Border>

            <Border Grid.Column="2" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock x:Name="SelectionTitle" Margin="4,2,4,9" FontSize="15"
                               FontWeight="SemiBold" Text="Выберите узел дерева" />
                    <TextBox x:Name="DetailsTextBox" Grid.Row="1" IsReadOnly="True" AcceptsReturn="True"
                             TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto" FontFamily="Cascadia Mono, Consolas"
                             FontSize="12" Padding="11" />
                </Grid>
            </Border>
        </Grid>

        <Grid Grid.Row="3" Margin="0,14,0,0">
            <TextBlock x:Name="StatusText" VerticalAlignment="Center" FontSize="11"
                       Foreground="{StaticResource MutedTextBrush}" Text="Ожидание…" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="RefreshButton" Content="Обновить снимок" Padding="16,7"
                        Click="Refresh_Click" />
                <Button x:Name="CopyButton" Content="Копировать выбранное" Padding="16,7"
                        Margin="10,0,0,0" IsEnabled="False" Click="Copy_Click" />
                <Button Content="Закрыть" Padding="16,7" Margin="10,0,0,0" Click="Close_Click" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
