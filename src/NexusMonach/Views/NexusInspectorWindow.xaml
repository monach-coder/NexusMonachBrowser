<Window x:Class="NexusMonach.Views.NexusInspectorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:NexusMonach.Views"
        Title="Nexus Inspector" Width="1180" Height="760" MinWidth="820" MinHeight="520"
        Background="{StaticResource BackgroundBrush}" Foreground="{StaticResource TextBrush}"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="NEXUS INSPECTOR" FontSize="22" FontWeight="SemiBold"
                           Foreground="{StaticResource AccentBrush}" />
                <TextBlock x:Name="PageText" Margin="0,3,0,0" Foreground="{StaticResource MutedTextBrush}"
                           TextTrimming="CharacterEllipsis" />
            </StackPanel>
            <Button Grid.Column="1" Content="Обновить дерево" Margin="8,0" Padding="15,8"
                    Click="Refresh_Click" />
            <Button Grid.Column="2" Content="Копировать" Margin="0,0,8,0" Padding="15,8"
                    Click="Copy_Click" />
            <Button Grid.Column="3" Content="Chromium DevTools" Padding="15,8"
                    Click="ChromiumDevTools_Click" />
        </Grid>

        <Grid Grid.Row="1" Margin="0,12,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SearchBox" Padding="10,7" VerticalContentAlignment="Center"
                     ToolTip="Поиск по имени элемента, атрибутам, ресурсу или сетевому событию"
                     KeyDown="SearchBox_KeyDown" />
            <Button Grid.Column="1" Content="Найти" Margin="8,0,0,0" Padding="18,7"
                    Click="Find_Click" />
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.46*" MinWidth="300" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="0.54*" MinWidth="340" />
            </Grid.ColumnDefinitions>

            <Border Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1" CornerRadius="12" Padding="8">
                <TreeView x:Name="InspectorTree" Background="Transparent" BorderThickness="0"
                          Foreground="{StaticResource TextBrush}"
                          SelectedItemChanged="InspectorTree_SelectedItemChanged">
                    <TreeView.Resources>
                        <HierarchicalDataTemplate DataType="{x:Type local:InspectorNode}"
                                                  ItemsSource="{Binding Children}">
                            <StackPanel Orientation="Horizontal" Margin="1">
                                <TextBlock Text="{Binding Glyph}" Width="22"
                                           Foreground="{StaticResource AccentBrush}" />
                                <TextBlock Text="{Binding Header}" TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                        </HierarchicalDataTemplate>
                    </TreeView.Resources>
                    <TreeView.ItemContainerStyle>
                        <Style TargetType="TreeViewItem">
                            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
                            <Setter Property="Background" Value="Transparent" />
                            <EventSetter Event="Expanded" Handler="TreeItem_Expanded" />
                        </Style>
                    </TreeView.ItemContainerStyle>
                </TreeView>
            </Border>

            <GridSplitter Grid.Column="1" Width="8" Background="Transparent"
                          HorizontalAlignment="Stretch" />

            <Border Grid.Column="2" Background="{StaticResource PanelBrush}"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                    CornerRadius="12" Padding="12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock x:Name="SelectionTitle" Text="Выберите узел дерева"
                               FontSize="16" FontWeight="SemiBold" />
                    <TextBox x:Name="DetailsBox" Grid.Row="1" Margin="0,10,0,8"
                             Background="{StaticResource FieldBrush}" Foreground="{StaticResource TextBrush}"
                             BorderBrush="{StaticResource BorderBrush}" FontFamily="Cascadia Mono, Consolas"
                             FontSize="12" IsReadOnly="True" AcceptsReturn="True" TextWrapping="NoWrap"
                             HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" />
                    <TextBlock x:Name="StatusText" Grid.Row="2" Foreground="{StaticResource MutedTextBrush}"
                               Text="Готов" />
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
