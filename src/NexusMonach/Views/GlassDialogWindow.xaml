<Window x:Class="NexusMonach.Views.GlassDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nexus Monach" Width="520" Height="290" MinWidth="440" MinHeight="240"
        ResizeMode="NoResize" ShowInTaskbar="False" WindowStartupLocation="CenterOwner"
        Icon="/Assets/nexus-monach.ico" Style="{StaticResource GlassPopupWindow}">
    <Grid Margin="24,18,24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="54" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="IconText" FontFamily="Segoe UI Symbol" FontSize="31"
                       Foreground="{StaticResource AccentBrush}" VerticalAlignment="Top" />
            <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="MessageText" TextWrapping="Wrap" FontSize="14"
                           LineHeight="21" Foreground="White" />
            </ScrollViewer>
        </Grid>
        <StackPanel Grid.Row="1" Margin="0,18,0,0" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Content="ОК" MinWidth="96" Padding="15,8" Click="Ok_Click" />
            <Button x:Name="YesButton" Content="Да" MinWidth="96" Padding="15,8" Margin="8,0,0,0" Click="Yes_Click" />
            <Button x:Name="NoButton" Content="Нет" MinWidth="96" Padding="15,8" Margin="8,0,0,0" Click="No_Click" />
            <Button x:Name="CancelDialogButton" Content="Отмена" MinWidth="96" Padding="15,8" Margin="8,0,0,0" Click="Cancel_Click" />
        </StackPanel>
    </Grid>
</Window>
